---
apiVersion: eevee.bot/v1
kind: ipcconfig
metadata:
  name: {{ .Values.ipcConfig.name }}
  namespace: {{ include "eevee-bot.botNamespace" $ }}
  labels:
    {{- include "eevee-bot.labels" . | nindent 4 }}
spec:
  nats:
    {{- toYaml .Values.ipcConfig.nats | nindent 4 }}
